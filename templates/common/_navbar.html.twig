<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
        {#  utilisation de app.user de twig  #}
        {# {% if app.user is defined  %} #}
        {% if app.user   %}
        {#  as app_post_create #}
        <a class="nav-link active" aria-current="page" href="{{path("app_post_new")}}">create a post {{app.user.email}}</a>
        {# {{ app.user }} #}
        {% else %}
          <a class="nav-link active" aria-current="page" href="{{path("app_register")}}">SIGN UP</a>
        {% endif %}
        </li>
        <li class="nav-item">
          <a class="nav-link blog-header-logo text-dark " href="{{path("app_home")}}">INSTAG 2021</a>
        </li>
        <li class="nav-item">
        {# Home pour app_contact  #}
          <a class="nav-link" href="{{path("app_home")}}">Contact</a>
        </li>
      

        {% if app.user  %}
        <a class="nav-link active" aria-current="page" href="{{path("app_logout")}}">Log Out</a>
        {# {{ app.user }} #}
        {% else %}
          <a class="nav-link active" aria-current="page" href="{{path("app_login")}}">Log In</a>
        {% endif %}

        
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li>
      </ul>
    </div>
  </div>
</nav>